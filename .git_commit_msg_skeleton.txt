feat(ingest): emit and persist regex skeleton before LLM enrichment

Stream a skeleton-ready phase after chapterized regex extraction and temporal connascence, persist that baseline graph immediately, and update the timeline UI to show early readiness before long first-batch LLM processing.
