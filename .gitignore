# ---------- OS / Editors ----------
.DS_Store
*.swp
*.swo
*.orig
*.backup

# ---------- Root Node (avoid accidental npm) ----------
package-lock.json
npm-debug.log*
yarn-error.log*

# ---------- Build outputs ----------
build/
dist/
.cache/

# ---------- Python ----------
server/venv/
server/venv*/
server/**/__pycache__/
server/**/*.py[cod]
server/.pytest_cache/
*.egg-info/

# ---------- Secrets ----------
.env
*.env
server/.env
server/secure/log_encryption.key
server/**/log_encryption.key

# ---------- Data / Artifacts ----------
server/chroma_db/
*.sqlite3
# previously committed large datasets (keep out of git)
Loinc_*/
icd-data/
hp.json
phenotype.hpo*
*.zip

# ---------- Misc ----------
server/package.json
server/package-lock.json
# Python
**/__pycache__/
*.pyc
venv*/
# Node
node_modules/
server/node_modules/
# Builds & logs
build/
dist/
server/logs/
*.log
# macOS
.DS_Store
# secrets
.env
server/.env

# local snapshots / dumps
snapshots/
*.sql
# allow our versioned SQL
!database/sql/*.sql
!database/schemas/*.sql

# editor / temp
*.swp
*~
*.bak
*.bak.*

# big vendor datasets & working folders
data/SnomedCT_*/
data/Orphadata/
data/chv/
data/clinvar/
data/gwas/
data/n2c2/
physionet.org/
data/who/

# batch scratch & backups
data/disgenet_batches/
data/disgenet_curated.tsv.bak.*
data/*_ids
data/*_ids.*
data/*_ids.tsv
data/*_ids.txt
data/*_symbols.txt
data/*_ranked*.tsv

# data dumps
/data/**
*/seed_*.csv
# CHV/MIMIC build outputs & local artifacts
backups/
db_integrity_reports/
reports/
models/
nano
failed_ids.txt

# build artifacts
*.csv
*.pdf
=*
docker/certs-local/

# --- Local EoH / infra artifacts (do NOT commit) ---
docker_env_report/
mkg_backup_*.dump

# Temp scratch dirs/files
extracting
reading
resolve
transferring

# Hash / checksum temp files
sha256:*

# Local auth/internal scratch (escaped brackets so Git doesn't treat them as char classes)
\[auth\]/
\[internal\]/

# Nested repo companion
ai_code_pipelines/