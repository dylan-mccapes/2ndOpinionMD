/**
 * 2OPMD Mobile — Theme System
 *
 * Dark mode first. Supports system light/dark toggle.
 * Colors, typography, spacing, radius, shadows all from tokens.
 */

export { colors, typography, spacing, radius, shadows, components } from './tokens';
