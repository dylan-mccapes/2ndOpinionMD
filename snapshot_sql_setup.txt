# SQL Setup Snapshot - 2ndOpinionMD-MVP
Generated: August 03, 2025 21:34:06 UTC

## Overview
The repository implements a sophisticated dual-database architecture with PostgreSQL (primary knowledge graph) and MongoDB (user data), featuring advanced vector search capabilities through pgvector extension and comprehensive medical ontology schemas.

## Database Architecture

### Primary Database: PostgreSQL with pgvector
**Purpose**: Medical knowledge graph storage with vector similarity search
**Extensions**: pgvector for embedding storage and cosine similarity operations
**Schema Evolution**: From basic ontology tables to unified cross-system support

### Secondary Database: MongoDB
**Purpose**: User data, authentication, and journal entries
**Integration**: Fallback support in PostgreSQL for unified data access
**Models**: Pydantic-based with flexible JSON storage

## Schema Evolution

### Generation 1: Basic Knowledge Graph (setup_knowledgegraph.sql)

#### Core Ontology Schema
```sql
-- Basic ICD hierarchy table
CREATE TABLE ontology.icd (
    id SERIAL PRIMARY KEY,
    code VARCHAR(20) NOT NULL,
    short_description TEXT,
    long_description TEXT,
    parent_code VARCHAR(20),
    full_path TEXT,
    version VARCHAR(10) DEFAULT '2024',
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW()
);

-- Self-referencing foreign key for hierarchy
ALTER TABLE ontology.icd 
ADD CONSTRAINT fk_icd_parent 
FOREIGN KEY (parent_code) REFERENCES ontology.icd(code);

-- Performance indexes
CREATE INDEX idx_icd_parent_code ON ontology.icd(parent_code);
CREATE INDEX idx_icd_version ON ontology.icd(version);
CREATE INDEX idx_icd_full_path ON ontology.icd(full_path);
```

#### Schema Organization
```sql
-- Dedicated ontology schema for medical terminologies
CREATE SCHEMA IF NOT EXISTS ontology;

-- Future expansion ready
-- CREATE SCHEMA IF NOT EXISTS ehr;      -- Electronic Health Records
-- CREATE SCHEMA IF NOT EXISTS text;     -- Text processing and NLP
-- CREATE SCHEMA IF NOT EXISTS molecular; -- Molecular and genetic data
```

### Generation 2: Unified Vector-Enabled Schema (unified_icd_schema.sql)

#### Advanced Ontology Table
```sql
-- pgvector extension for embedding storage
CREATE EXTENSION IF NOT EXISTS vector;

-- Unified table supporting multiple ontology systems
CREATE TABLE ontology.icd (
    id SERIAL PRIMARY KEY,
    system VARCHAR(20) NOT NULL,           -- 'ICD-10-CM', 'ICD-11'
    version VARCHAR(10),                   -- '2024', '2025', etc.
    code VARCHAR(20) NOT NULL,             -- Ontology code
    short_description TEXT,                -- Brief description
    long_description TEXT,                 -- Detailed description
    parent_code VARCHAR(20),               -- Hierarchical parent
    full_path TEXT,                        -- Breadcrumb navigation
    class_kind VARCHAR(50),                -- ICD-11: chapter, block, category
    foundation_uri TEXT,                   -- ICD-11: Foundation URI
    linearization_uri TEXT,                -- ICD-11: Linearization URI
    term_vector vector(1536),              -- OpenAI embeddings (primary)
    term_vector_json JSON,                 -- JSON fallback for embeddings
    metadata JSONB,                        -- Extensible metadata storage
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW()
);
```

#### Cross-System Mapping
```sql
-- Ontology cross-reference table for system interoperability
CREATE TABLE ontology.cross_reference (
    id SERIAL PRIMARY KEY,
    source_system VARCHAR(20) NOT NULL,
    source_code VARCHAR(20) NOT NULL,
    target_system VARCHAR(20) NOT NULL,
    target_code VARCHAR(20) NOT NULL,
    mapping_type VARCHAR(20) NOT NULL,     -- 'exact', 'broader', 'narrower'
    confidence_score DECIMAL(3,2),         -- 0.00 to 1.00
    created_at TIMESTAMP DEFAULT NOW()
);
```

#### Advanced Indexing Strategy
```sql
-- Hierarchy and navigation indexes
CREATE INDEX idx_icd_system_version ON ontology.icd(system, version);
CREATE INDEX idx_icd_parent_code ON ontology.icd(parent_code);
CREATE INDEX idx_icd_full_path ON ontology.icd(full_path);
CREATE INDEX idx_icd_class_kind ON ontology.icd(class_kind);

-- Vector similarity search indexes
CREATE INDEX idx_icd_term_vector ON ontology.icd 
USING ivfflat (term_vector vector_cosine_ops) WITH (lists = 100);

-- Cross-reference indexes
CREATE INDEX idx_cross_ref_source ON ontology.cross_reference(source_system, source_code);
CREATE INDEX idx_cross_ref_target ON ontology.cross_reference(target_system, target_code);

-- Metadata search indexes
CREATE INDEX idx_icd_metadata ON ontology.icd USING GIN (metadata);
```

#### Constraints and Validation
```sql
-- System validation
ALTER TABLE ontology.icd 
ADD CONSTRAINT chk_system 
CHECK (system IN ('ICD-10-CM', 'ICD-11', 'SNOMED-CT', 'HPO'));

-- Confidence score validation
ALTER TABLE ontology.cross_reference 
ADD CONSTRAINT chk_confidence 
CHECK (confidence_score >= 0.00 AND confidence_score <= 1.00);

-- Mapping type validation
ALTER TABLE ontology.cross_reference 
ADD CONSTRAINT chk_mapping_type 
CHECK (mapping_type IN ('exact', 'broader', 'narrower', 'related'));
```

## PostgreSQL Models (SQLAlchemy)

### User Management
```python
class User(Base):
    __tablename__ = "users"
    
    id = Column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
    email = Column(String, unique=True, index=True, nullable=False)
    full_name = Column(String, nullable=False)
    hashed_password = Column(String, nullable=False)
    birthdate = Column(DateTime, nullable=True)
    subscription_tier = Column(String, default="basic")
    created_at = Column(DateTime, default=datetime.utcnow)
    last_login = Column(DateTime, nullable=True)
    
    # Security features
    is_verified = Column(Boolean, default=False)
    verification_token = Column(String, nullable=True)
    verification_token_expires = Column(DateTime, nullable=True)
    failed_login_attempts = Column(Integer, default=0)
    locked_until = Column(DateTime, nullable=True)
    password_reset_token = Column(String, nullable=True)
    password_reset_token_expires = Column(DateTime, nullable=True)
    
    journal_entries = relationship("JournalEntry", back_populates="user")
```

### Journal System
```python
class JournalEntry(Base):
    __tablename__ = "journal_entries"
    
    id = Column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
    user_id = Column(UUID(as_uuid=True), ForeignKey("users.id"), nullable=False)
    date = Column(DateTime, default=datetime.utcnow)
    
    # Medical data (JSON storage for flexibility)
    symptoms = Column(JSON)                    # Array of symptom objects
    environmental_factors = Column(JSON)       # Array of environmental factors
    stress_level = Column(Integer, nullable=True)      # 1-10 scale
    diet_notes = Column(Text, nullable=True)
    sleep_quality = Column(Integer, nullable=True)     # 1-10 scale
    notes = Column(Text, nullable=True)
    
    # AI analysis results
    analysis = Column(Text, nullable=True)
    pattern_observations = Column(Text, nullable=True)
    ai_analysis = Column(JSON, nullable=True)
    
    created_at = Column(DateTime, default=datetime.utcnow)
    updated_at = Column(DateTime, nullable=True)
    
    user = relationship("User", back_populates="journal_entries")
```

### Medical Knowledge Storage
```python
class MedicalKnowledge(Base):
    __tablename__ = "medical_knowledge"
    
    id = Column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
    content_type = Column(String, nullable=False)  # disease, case, condition, etc.
    title = Column(String, nullable=False)
    content = Column(Text, nullable=False)
    icd10_code = Column(String, nullable=True)
    meta_data = Column(JSON)
    embedding = Column(Vector(1536))  # OpenAI embedding dimension
    created_at = Column(DateTime, default=datetime.utcnow)
```

## MongoDB Models (Pydantic)

### User Models
```python
class UserInDB(UserBase):
    id: str = Field(default_factory=lambda: str(datetime.now().timestamp()))
    hashed_password: str
    subscription_tier: str = "basic"  # basic, premium, professional
    created_at: datetime = Field(default_factory=datetime.now)
    last_login: Optional[datetime] = None
    
    # Security features
    is_verified: bool = False
    verification_token: Optional[str] = None
    verification_token_expires: Optional[datetime] = None
    failed_login_attempts: int = 0
    locked_until: Optional[datetime] = None
    password_reset_token: Optional[str] = None
    password_reset_token_expires: Optional[datetime] = None
```

### Journal Models
```python
class SymptomEntry(BaseModel):
    symptom: str
    severity: int = Field(ge=1, le=10)  # 1-10 scale

class EnvironmentalFactor(BaseModel):
    factor_type: str  # food, product, environment, medication, other
    description: str

class JournalEntry(JournalEntryBase):
    id: str = Field(default_factory=lambda: str(datetime.now().timestamp()))
    user_id: str
    created_at: datetime = Field(default_factory=datetime.now)
    updated_at: Optional[datetime] = None
    ai_analysis: Optional[Dict[str, Any]] = None
```

## Vector Search Implementation

### pgvector Configuration
```sql
-- Vector similarity search using cosine distance
SELECT id, content, title, 
       embedding <-> '[0.1,0.2,0.3,...]'::vector as distance
FROM medical_knowledge
ORDER BY embedding <-> '[0.1,0.2,0.3,...]'::vector
LIMIT 10;

-- Index optimization for large datasets
CREATE INDEX ON medical_knowledge 
USING ivfflat (embedding vector_cosine_ops) 
WITH (lists = 100);
```

### Query Engine Integration
```python
async def query_medical_knowledge(self, query: str, content_types: Optional[List[str]] = None, top_k: int = 5):
    query_embedding = await self.get_embedding(query)
    
    async with async_session() as session:
        if isinstance(query_embedding, list):
            embedding_str = '[' + ','.join(map(str, query_embedding)) + ']'
        else:
            embedding_str = str(query_embedding)
        
        sql = f"""
        SELECT id, content_type, title, content, icd10_code, meta_data,
               embedding <-> '{embedding_str}'::vector as distance
        FROM medical_knowledge
        ORDER BY embedding <-> '{embedding_str}'::vector 
        LIMIT {top_k}
        """
        
        result = await session.execute(text(sql))
        return result.fetchall()
```

## Database Connection Management

### Async PostgreSQL Setup
```python
from sqlalchemy.ext.asyncio import create_async_engine, AsyncSession
from sqlalchemy.orm import sessionmaker

DATABASE_URL = "postgresql+asyncpg://user:password@localhost/dbname"

engine = create_async_engine(DATABASE_URL, echo=True)
async_session = sessionmaker(engine, class_=AsyncSession, expire_on_commit=False)

async def init_db():
    async with engine.begin() as conn:
        await conn.run_sync(Base.metadata.create_all)
```

### MongoDB Connection
```python
from motor.motor_asyncio import AsyncIOMotorClient

MONGODB_URL = "mongodb://localhost:27017"
client = AsyncIOMotorClient(MONGODB_URL)
database = client.medical_app
```

## Migration Strategy

### Schema Versioning
```sql
-- Version tracking table
CREATE TABLE schema_migrations (
    version VARCHAR(20) PRIMARY KEY,
    applied_at TIMESTAMP DEFAULT NOW(),
    description TEXT
);

-- Example migration tracking
INSERT INTO schema_migrations (version, description) 
VALUES ('001_basic_ontology', 'Initial ICD hierarchy table');

INSERT INTO schema_migrations (version, description) 
VALUES ('002_vector_support', 'Added pgvector and unified schema');
```

### Data Migration Scripts
```python
# Migration from basic to unified schema
async def migrate_to_unified_schema():
    # 1. Create new unified table
    # 2. Migrate existing data with system='ICD-10-CM'
    # 3. Generate embeddings for existing records
    # 4. Update foreign key references
    # 5. Drop old table after validation
```

## Performance Optimization

### Query Optimization
```sql
-- Materialized views for common queries
CREATE MATERIALIZED VIEW ontology.icd_hierarchy AS
SELECT 
    code,
    short_description,
    parent_code,
    full_path,
    array_length(string_to_array(full_path, ' > '), 1) as depth_level
FROM ontology.icd
WHERE system = 'ICD-10-CM';

-- Refresh strategy
REFRESH MATERIALIZED VIEW ontology.icd_hierarchy;
```

### Connection Pooling
```python
# PostgreSQL connection pool
engine = create_async_engine(
    DATABASE_URL,
    pool_size=20,
    max_overflow=30,
    pool_pre_ping=True,
    pool_recycle=3600
)
```

## Security Considerations

### Row Level Security
```sql
-- Enable RLS for user data
ALTER TABLE journal_entries ENABLE ROW LEVEL SECURITY;

-- Policy for user data access
CREATE POLICY user_journal_policy ON journal_entries
FOR ALL TO authenticated_users
USING (user_id = current_user_id());
```

### Data Encryption
```sql
-- Sensitive data encryption
CREATE EXTENSION IF NOT EXISTS pgcrypto;

-- Encrypted storage for sensitive fields
ALTER TABLE users 
ADD COLUMN encrypted_notes TEXT;

-- Encryption/decryption functions
UPDATE users 
SET encrypted_notes = pgp_sym_encrypt(notes, 'encryption_key');
```

## Backup and Recovery

### Automated Backup Strategy
```bash
# Daily PostgreSQL backup
pg_dump -h localhost -U postgres -d medical_db \
  --format=custom --compress=9 \
  --file=backup_$(date +%Y%m%d).dump

# MongoDB backup
mongodump --host localhost:27017 --db medical_app \
  --out backup_$(date +%Y%m%d)
```

### Point-in-Time Recovery
```sql
-- WAL archiving configuration
archive_mode = on
archive_command = 'cp %p /backup/archive/%f'
wal_level = replica
```

This SQL setup provides a robust foundation for medical knowledge graph operations with advanced vector search capabilities, comprehensive user management, and scalable architecture ready for multi-ontology integration.
